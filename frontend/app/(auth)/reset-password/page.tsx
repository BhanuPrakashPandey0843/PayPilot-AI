import { PagePlaceholder } from "../../_components/PagePlaceholder";

export default function ResetPasswordPage() {
  return (
    <PagePlaceholder
      title="Reset Password"
      description="New password, confirm password, success message, back to login."
    />
  );
}
