import { PagePlaceholder } from "../../_components/PagePlaceholder";

export default function SignUpPage() {
  return (
    <PagePlaceholder
      title="Sign Up"
      description="Org name, org slug, name, email, password, confirm password."
    />
  );
}
