import { PagePlaceholder } from "../../_components/PagePlaceholder";

export default function DemoPage() {
  return (
    <PagePlaceholder
      title="Demo"
      description="Product walkthrough, key flows, use cases, screens / video."
    />
  );
}
